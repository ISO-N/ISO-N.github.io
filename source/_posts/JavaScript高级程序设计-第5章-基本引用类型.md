---
title: 第5章 基本引用类型
date: 2026-02-17 15:01:59
categories:
- [编程技术, JavaScript]
tags:
- JavaScript高级程序设计
---
引用类型是把数据和功能组织到一起的结构，虽然常被称为"类"，但 ECMAScript 本质上是**基于原型**的语言。引用值（对象）是某个特定引用类型的**实例**，通过 `new` 操作符加构造函数来创建。

---

## 5.1 Date

`Date` 类型参考了 Java 早期的 `java.util.Date`，将日期保存为自 **UTC 1970 年 1 月 1 日零时** 起经过的**毫秒数**。

### 创建日期对象

```jsx
// 当前日期和时间
let now = new Date();

// 基于特定日期 —— 使用 Date.parse() 或 Date.UTC()
let date1 = new Date(Date.parse("May 23, 2019"));
let date2 = new Date("May 23, 2019"); // 隐式调用 Date.parse()

// Date.UTC()：参数为年、零起点月（0=一月）、日、时、分、秒、毫秒
let date3 = new Date(Date.UTC(2000, 0));          // 2000-01-01T00:00:00Z
let date4 = new Date(Date.UTC(2005, 4, 5, 17, 55, 55)); // 2005-05-05T17:55:55Z

// 直接给 Date 构造函数传与 Date.UTC() 相同的参数 → 创建的是本地时间
let date5 = new Date(2000, 0); // 本地时间 2000-01-01 00:00
```

### [Date.now](http://Date.now)()

返回执行时的毫秒数，可用于代码计时：

```jsx
let start = Date.now();
doSomething();
let stop = Date.now();
let elapsed = stop - start;
```

### 继承的方法

| 方法 | 说明 |
| --- | --- |
| `toLocaleString()` | 返回与浏览器本地环境一致的日期和时间，通常包含 AM/PM，不含时区 |
| `toString()` | 返回带时区信息的日期和时间 |
| `valueOf()` | 返回毫秒数，因此可以直接用 `<` `>` 比较日期先后 |

### 日期格式化方法

- `toDateString()` — 显示日期中的周几、月、日、年
- `toTimeString()` — 显示日期中的时、分、秒和时区
- `toLocaleDateString()` / `toLocaleTimeString()` — 本地化的日期 / 时间
- `toUTCString()` — 显示完整的 UTC 日期

### 日期 / 时间组件方法（常用）

| 方法 | 说明 |
| --- | --- |
| `getFullYear()` / `setFullYear()` | 4 位数年份 |
| `getMonth()` / `setMonth()` | 月（0~11） |
| `getDate()` / `setDate()` | 日（1~31） |
| `getDay()` | 周几（0=周日） |
| `getHours()` / `setHours()` | 时（0~23） |
| `getMinutes()` / `setMinutes()` | 分（0~59） |
| `getSeconds()` / `setSeconds()` | 秒（0~59） |
| `getMilliseconds()` / `setMilliseconds()` | 毫秒 |
| `getTime()` / `setTime()` | 日期的毫秒表示 |
| `getTimezoneOffset()` | 本地时间与 UTC 的分钟差 |

> 以上方法均有对应的 UTC 版本，如 `getUTCFullYear()`。
> 

---

## 5.2 RegExp

ECMAScript 通过 `RegExp` 类型支持正则表达式。

### 创建正则

```jsx
// 字面量
let pattern1 = /at/g;            // 全局匹配 "at"
let pattern2 = /[bc]at/i;        // 不区分大小写匹配 "bat" 或 "cat"
let pattern3 = /.at/gi;          // 全局 + 不区分大小写

// 构造函数（接受字符串，元字符需双重转义）
let pattern4 = new RegExp("[bc]at", "i");
let pattern5 = new RegExp("\\[bc\\]at", "i"); // 匹配 "[bc]at"
```

### 标记（flags）

| 标记 | 说明 |
| --- | --- |
| `g` | 全局模式，查找所有匹配 |
| `i` | 不区分大小写 |
| `m` | 多行模式，`^` `$` 匹配每行的开头和末尾 |
| `y` | 粘附模式，只从 `lastIndex` 开始匹配 |
| `u` | Unicode 模式，启用 Unicode 匹配 |
| `s` | dotAll 模式，`.` 匹配任意字符（包括 `\n`） |

### 实例属性

- `global` / `ignoreCase` / `multiline` / `unicode` / `sticky` / `dotAll` — 布尔值，表示对应标记是否设置
- `lastIndex` — 下一次搜索开始的位置（整数）
- `source` — 正则表达式的字面量字符串
- `flags` — 标记字符串

### 实例方法

**`exec()`** — 核心方法，返回包含第一个匹配信息的数组（含 `index` 和 `input` 属性），无匹配返回 `null`：

```jsx
let text = "mom and dad and baby";
let pattern = /mom( and dad( and baby)?)?/gi;
let matches = pattern.exec(text);
// matches[0] = "mom and dad and baby"（整体匹配）
// matches[1] = " and dad and baby"（第一个捕获组）
// matches[2] = " and baby"（第二个捕获组）
// matches.index = 0
```

<aside>
💡

设置了 `g` 标记后，每次调用 `exec()` 都会在字符串中向前搜索下一个匹配（`lastIndex` 自动更新）；不设 `g` 则每次都从头开始。`y` 标记类似，但如果 `lastIndex` 位置没有匹配就返回 `null`。

</aside>

**`test()`** — 测试是否匹配，返回布尔值：

```jsx
let pattern = /\d{3}-\d{2}-\d{4}/;
if (pattern.test("000-00-0000")) {
  console.log("匹配成功");
}
```

### RegExp 构造函数属性（静态属性）

| 全名 | 简写 | 说明 |
| --- | --- | --- |
| `input` | `$_` | 最后搜索的字符串 |
| `lastMatch` | `$&` | 最后匹配的文本 |
| `lastParen` | `$+` | 最后匹配的捕获组 |
| `leftContext` |  $ ``` | 匹配文本左侧的文本 |
| `rightContext` | `$'` | 匹配文本右侧的文本 |

> `RegExp.$1` ~ `RegExp.$9` 存储第 1~9 个捕获组的匹配。
> 

---

## 5.3 原始值包装类型

每当用到某个原始值的方法或属性时，后台会创建一个对应的**原始值包装对象**，执行完毕后立即销毁。ECMAScript 提供了三种包装类型：`Boolean`、`Number`、`String`。

```jsx
let s1 = "some text";
let s2 = s1.substring(2);
// 等价于：
// let s1 = new String("some text");
// let s2 = s1.substring(2);
// s1 = null;   ← 自动销毁
```

<aside>
⚠️

手动用 `new Boolean()` / `new Number()` / `new String()` 创建的包装对象会导致 `typeof` 返回 `"object"`，布尔表达式中始终为 `true`，容易引起混淆，**不推荐显式使用**。

</aside>

### 5.3.1 Boolean

`Boolean` 的实例重写了 `valueOf()`（返回 `true`/`false`）和 `toString()`（返回 `"true"`/`"false"`）。

```jsx
let falseObj = new Boolean(false);
// falseObj 是对象，在布尔上下文中为 true！
if (falseObj) {
  console.log("这会执行！"); // ✅
}
```

### 5.3.2 Number

常用方法：

| 方法 | 说明 | 示例 |
| --- | --- | --- |
| `toFixed(n)` | 返回包含 n 位小数的字符串 | `(10.005).toFixed(2)` → `"10.01"` |
| `toExponential(n)` | 返回科学记数法，n 位小数 | `(10).toExponential(1)` → `"1.0e+1"` |
| `toPrecision(n)` | 根据值自动选择 fixed 或 exponential，总共 n 位有效数字 | `(99).toPrecision(1)` → `"1e+2"` |
| `Number.isInteger()` | 判断是否为整数 | `Number.isInteger(1.00)` → `true` |
| `Number.isSafeInteger()` | 判断是否在安全整数范围 ±2⁵³ 内 | — |

### 5.3.3 String

#### 字符方法

| 方法 | 说明 |
| --- | --- |
| `charAt(index)` | 返回给定索引位置的字符 |
| `charCodeAt(index)` | 返回字符的 UTF-16 码元值 |
| `codePointAt(index)` | 返回完整的 Unicode 码点（支持代理对） |
| `String.fromCharCode()` | 根据 UTF-16 码元创建字符 |
| `String.fromCodePoint()` | 根据 Unicode 码点创建字符 |

#### `normalize()` 方法

Unicode 同一个字符可能有多种编码方式。`normalize()` 将字符串规范化为指定形式：`"NFC"`（默认）、`"NFD"`、`"NFKC"`、`"NFKD"`。

#### 字符串操作方法

| 方法 | 说明 |
| --- | --- |
| `concat()` | 拼接字符串（更常用 `+`） |
| `slice(start, end)` | 提取子串；负参数 = length + 负值 |
| `substring(start, end)` | 提取子串；负参数被视为 0 |
| `substr(start, length)` | 从 start 提取 length 个字符（已不推荐） |

#### 字符串位置方法

- `indexOf(str, fromIndex)` — 从前向后搜索
- `lastIndexOf(str, fromIndex)` — 从后向前搜索
- 均返回首次出现的位置，未找到返回 `-1`

#### 字符串包含方法（ES6+）

| 方法 | 说明 |
| --- | --- |
| `startsWith(str, pos)` | 是否以 str 开头 |
| `endsWith(str, pos)` | 是否以 str 结尾 |
| `includes(str, pos)` | 是否包含 str |

#### `trim()` 方法

- `trim()` — 去除首尾空格
- `trimStart()` / `trimLeft()` — 去除开头空格
- `trimEnd()` / `trimRight()` — 去除末尾空格

#### `repeat()` 方法

```jsx
"na ".repeat(3); // "na na na "
```

#### `padStart()` / `padEnd()`

```jsx
"foo".padStart(6);       // "   foo"
"foo".padStart(6, ".");  // "...foo"
"foo".padEnd(8, "bar");  // "foobarba"
```

#### 字符串迭代与解构

```jsx
// for-of 遍历字符（支持代理对）
for (const c of "abc") { console.log(c); }

// 解构
let chars = [..."ab☺c"]; // ["a", "b", "☺", "c"]
```

#### 大小写转换

- `toLowerCase()` / `toUpperCase()`
- `toLocaleLowerCase()` / `toLocaleUpperCase()`（针对特定地区）

#### 字符串模式匹配方法

| 方法 | 说明 |
| --- | --- |
| `match(pattern)` | 与 `RegExp.exec()` 类似，返回匹配数组 |
| `matchAll(pattern)` | 返回所有匹配的迭代器（要求 `g` 标记） |
| `search(pattern)` | 返回第一个匹配的索引，无匹配返回 -1 |
| `replace(pattern, replacement)` | 替换匹配的子串 |
| `replaceAll(pattern, replacement)` | 替换所有匹配（ES2021） |
| `split(separator, limit)` | 按分隔符拆分为数组 |

`replace()` 的第二个参数可以使用**特殊序列**：

| 序列 | 说明 |
| --- | --- |
| `$$` | 插入 `$` |
| `$&` | 整个匹配 |
| `$'` | 匹配之后的文本 |
|  $ ``` | 匹配之前的文本 |
| `$n` | 第 n 个捕获组（n=1~99） |

第二个参数也可以是**函数**，接收 `(match, ...captures, index, input)` → 返回替换字符串。

#### `localeCompare()`

按本地排序规则比较字符串，返回负数、0 或正数。

---

## 5.4 单例内置对象

"内置对象"指由 ECMAScript 规范定义、不依赖宿主环境、在程序执行前就已存在的对象。除了 `Object`、`Array`、`String` 等，还有两个单例内置对象：**Global** 和 **Math**。

### 5.4.1 Global

`Global` 是兜底对象——不属于任何对象的函数和属性都是 `Global` 的。

#### URL 编码方法

| 方法 | 说明 |
| --- | --- |
| `encodeURI()` | 编码整个 URI，**不编码**特殊字符（`:`、`/`、`?`、`#` 等） |
| `encodeURIComponent()` | 编码 URI 组件，**编码所有**非标准字符 |
| `decodeURI()` / `decodeURIComponent()` | 对应的解码方法 |

#### `eval()`

```jsx
eval("console.log('hi')"); // 等同于直接写 console.log('hi')
```

<aside>
⚠️

`eval()` 中的代码拥有与调用上下文相同的作用域链和变量对象，可以访问和定义外部变量。在严格模式下，`eval` 内部声明的变量和函数无法被外部访问。由于安全性和性能问题，**应尽量避免使用**。

</aside>

#### Global 对象属性

包括 `undefined`、`NaN`、`Infinity`，以及所有原生引用类型的构造函数（`Object`、`Array`、`Function`、`Boolean`、`String`、`Number`、`Date`、`RegExp`、`Symbol`、`Error` 等）。

#### window 对象

浏览器中，`window` 对象被实现为 Global 对象的代理。全局作用域中声明的变量和函数都成为 `window` 的属性：

```jsx
var color = "red";
console.log(window.color); // "red"
```

### 5.4.2 Math

`Math` 对象提供数学计算的属性和方法。

#### 常用属性

`Math.E`、`Math.LN10`、`Math.LN2`、`Math.LOG2E`、`Math.LOG10E`、`Math.PI`、`Math.SQRT1_2`、`Math.SQRT2`

#### `min()` / `max()`

```jsx
Math.max(3, 54, 32, 16);         // 54
Math.min(3, 54, 32, 16);         // 3
Math.max(...[1, 2, 3]);          // 3（配合展开操作符）
```

#### 舍入方法

| 方法 | 说明 | 示例 |
| --- | --- | --- |
| `Math.ceil()` | 向上取整 | `Math.ceil(25.1)` → `26` |
| `Math.floor()` | 向下取整 | `Math.floor(25.9)` → `25` |
| `Math.round()` | 四舍五入 | `Math.round(25.5)` → `26` |
| `Math.fround()` | 返回最接近的单精度浮点值 | `Math.fround(0.4)` → `0.4000000059604645` |

#### `random()`

返回 `[0, 1)` 之间的随机数：

```jsx
// 获取 [lowerValue, upperValue] 之间的随机整数
function randomInt(lowerValue, upperValue) {
  let choices = upperValue - lowerValue + 1;
  return Math.floor(Math.random() * choices + lowerValue);
}
randomInt(2, 10); // 2~10 之间的随机整数
```

#### 其他方法

| 方法 | 说明 |
| --- | --- |
| `Math.abs(x)` | 绝对值 |
| `Math.pow(x, n)` | x 的 n 次幂 |
| `Math.sqrt(x)` | 平方根 |
| `Math.cbrt(x)` | 立方根 |
| `Math.hypot(...vals)` | 所有参数平方和的平方根 |
| `Math.log(x)` | 自然对数 |
| `Math.log2(x)` / `Math.log10(x)` | 以 2 / 10 为底的对数 |
| `Math.trunc(x)` | 去除小数部分 |
| `Math.sign(x)` | 返回 -1、0、1 表示符号 |
| `Math.sin()` / `Math.cos()` / `Math.tan()` | 三角函数 |
| `Math.asin()` / `Math.acos()` / `Math.atan()` | 反三角函数 |

---

## 5.5 小结

<aside>
📝

- 引用类型与传统面向对象中的"类"相似，但实现不同
- **Date** 类型基于毫秒表示日期，提供大量组件方法
- **RegExp** 类型支持正则表达式，核心方法是 `exec()` 和 `test()`
- JavaScript 有 3 种**原始值包装类型**（Boolean、Number、String），每次访问原始值的属性时会临时创建包装对象
- **String** 类型方法极其丰富：字符访问、搜索、模式匹配、截取、填充、迭代等
- **Global** 是兜底对象，承载 `eval()`、URI 编码方法和所有原生构造函数；在浏览器中通过 `window` 暴露
- **Math** 对象提供数学常量和常用运算方法
</aside>